buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
    }
}

apply plugin: 'java'

repositories {
   mavenCentral()
}

task(run, dependsOn: 'classes', type: JavaExec) {
  main = 'Driver'
  classpath = sourceSets.main.runtimeClasspath
}
